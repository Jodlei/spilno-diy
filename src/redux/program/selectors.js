export const selectPrograms = (state) => state.program.listProgram;
export const selectIsLoading = (state) => state.program.isLoading;
export const selectProgramDetails = (state) => state.program.programDetails;
